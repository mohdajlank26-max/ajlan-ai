AJLAN AI — LOCAL VERSION
1. Open CMD in this folder.
2. Run: npm install
3. Run: npm start
4. Open http://localhost:3000
Ollama must be installed and the llama3.2:3b model must be downloaded.
Enter sends. Shift+Enter makes a new line.
This version does not need an OpenAI API key.